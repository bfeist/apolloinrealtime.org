# 08 — Progress tracker

This is the ground truth for the current state of the AiRT2 migration.
Every agent session must read this first and update it before ending.

---

## ⏩ Resume here

**Status (2026-05-29 — URL swap + restore session):** Phases 1–5 substantially complete. The typed app now lives at `/11/`, `/13/`, `/17/` (it took over what used to be the lifted-legacy URLs). The byte-for-byte legacy oracle lives at `/legacy/11/`, `/legacy/13/`, `/legacy/17/` via a dev-only Vite plugin. `npm run check` green; 238/238 tests; build green.

**Critical correction this session:** during Phases 1–2 the lifted legacy code in `public/{N}/` and the lifted `{N}/index.html` files were edited in place (the `index.js` files were rewritten to read `window.MISSION` instead of inlined constants; the `<head>` blocks were emptied so a Vite plugin could reinject them). This was a violation of the "legacy code is reference-only" rule and effectively corrupted the oracle. All three missions have now been **restored verbatim from the sibling repos** at `F:/_repos/Apollo_11`, `F:/_repos/Apollo_13`, `F:/_repos/Apollo17.org`. The `var c*` blocks are inline again, the original hand-crafted heads are back, and `/legacy/{N}/` matches `apolloinrealtime.org/{N}/` to the byte (modulo CRLF on a handful of files, which is harmless). The dropped subdirs are `mobile/` (all), `spacecraft_dev/` (A13), `nominee/` (A17) per the locked decisions in [00-decisions.md](00-decisions.md).

**URL strategy now (binding):** see [05-migration-plan.md §Strategy](05-migration-plan.md#strategy-binding--read-before-phase-work). The typed app lives at `/{N}/` from now on. No more `/dev/{N}/`. The atomic Phase 7 "swap URLs" step is gone — cutover is just shipping `dist/`. The legacy oracle is dev-only scaffolding and never ships.

Typed modules landed so far (all wired into `src/app/missionApp.ts` and served at `/{N}/`):

- `src/shell/clock.ts` (28 tests)
- `src/data/csvLoader.ts` (13 tests)
- `src/engines/ytplayer/index.ts` (3 tests)
- `src/engines/navigator/layout.ts` (41 tests) — pure layout/coord/hit-testing
- `src/engines/navigator/renderer.ts` + `paperApi.ts` (16 tests) — Paper.js drawing layer. Tiers + nav boxes + cursor + hover nav-cursor + click-to-seek + **data overlays**: mission-stage ticks, video-segment rectangles, photo ticks, TOC ticks + level-1 labels, half-hour time ticks (all in tiers 1 and 2). Overlays driven by `NavigatorOverlays` option; absent = skeleton-only (existing tests unaffected). `missionHarness.ts` loads the 4 overlay datasets concurrently and passes them in.
- `src/data/tocData.ts` (14 tests) — `indexes/TOCData.csv` loader/parser + `findClosestTocIndex`.
- `src/data/missionStagesData.ts` (10 tests) — `indexes/missionStagesData.csv` + `findStageIndex`.
- `src/data/videoSegmentData.ts` (13 tests) — `indexes/videoSegmentData.csv` + `findVideoSegmentIndex`.
- `src/data/commentaryData.ts` (7 tests) — `indexes/commentaryData.csv` + `findClosestCommentaryIndex`. Handles 2-col A13 and 4-col A11/A17 schema variants.
- `src/data/utteranceData.ts` (5 tests) — `indexes/utteranceData.csv` + `findClosestUtteranceIndex`. 4-col pipe-split schema.
- `src/data/photoData.ts` (5 tests) — `indexes/photoData.csv` + `findClosestPhotoIndex`. 6-col pipe-split schema.
- `src/data/videoUrlData.ts` (6 tests) — `indexes/videoURLData.csv` + `findVideoUrlIndex`. 3 schema variants normalized (A11/A13/A17).
- `src/data/crewStatusData.ts` (4 tests) — `indexes/crewStatusData.csv` + `findCrewStatusIndex`. End-times backfilled; requires `missionDurationSeconds`.
- `src/data/telemetryData.ts` (4 tests) — `indexes/telemetryData.csv` + `findTelemetryIndex`. Velocity + distance for Earth/Moon; end-times backfilled.
- `src/data/orbitData.ts` (5 tests) — `indexes/orbitData.csv` + `findOrbitIndex`. Lunar orbit sequence (A17-only; other missions load gracefully as empty).
- `src/panels/toc/index.ts` (4 tests) — first Phase 5 Track C panel. Builds the legacy `TOC.html` DOM in-page (no iframe, no jQuery).
- **`src/panels/commentary/index.ts` (3 tests)** — `createCommentaryPanel({container, data, onSeek, attributionRenderer?, activeBackground?})` builds `#commentaryTable` rows mirroring legacy `repopulateCommentary`. `setActiveTimeId` replaces `scrollCommentaryToTimeId` (clear+set bg + `scrollIntoView`). Delegated row click replaces the inline `parent.seekToTime(...)`.
- **`src/panels/transcript/index.ts` (5 tests)** — `createTranscriptPanel(...)` + `utteranceTypeClass(code, speaker)` returns the legacy `utt_pao` / `utt_capcom` / `utt_mocr` / `utt_crew` classes used by `repopulateUtterances`. Same setActiveTimeId pattern; delegated click.
- **`src/panels/photo/index.ts` (4 tests)** — `createPhotoPanel({data, resolveUrls: PhotoUrlResolver, onSeek})` renders `#photoGallery` + `#photodiv` from the typed photo data. Pure `parseAsRollImg(photoId, missionDigit)` extracts roll/img for the legacy LPI thumb path. URL resolution is injected per mission via callback (harness uses `photoResolverFor(config)` switching A11/A13/A17 LPI / ALSJ / media-CDN paths). Native `loading="lazy"` replaces the jQuery `lazyload` plugin.
- **`src/panels/telemetry/index.ts` (8 tests)** — `createTelemetryPanel({data, frameRanges: FrameOfReferenceRange[]})` with `interpolateTelemetryField` (linear between non-NaN samples) + `computeTelemetryDisplay` (Earth/Moon selection + unit conversions: `FPS_TO_KPH`, `FPS_TO_MPH`, `FPS_TO_MACH`, `NM_TO_KM`). A13 frame ranges (Moon `[075:06:08, 085:59:00)`) hardcoded in `frameRangesFor(config)` in the harness; A11/A17 default to Earth pending real ranges.
- **`src/panels/crewStatus/index.ts` (5 tests)** — `createCrewStatusPanel({data})` + pure `timeToWakeup(entries, currentIndex, currentSeconds)`. Renders the curated `statusHtml` (legacy CSV column) and, when current status matches `/sleeping/i`, appends a "Wake-up in: HHH:MM:SS" countdown.
- **`src/panels/dashboard/index.ts` (3 tests)** — `createDashboardPanel({stages, crewStatus, telemetry, telemetryFrameRanges, totalMissionDays})` composes mission day (`missionDay(seconds) = floor(seconds/86400)+1`) + stage + child `createCrewStatusPanel` + child `createTelemetryPanel`. Replaces the legacy `updateDashboard` inline block.
- **`src/panels/search/index.ts` (7 tests)** — `buildSearchIndex({utterances, commentary, photos})` + pure `searchIndex(items, query, max)` (2+ char threshold, case-insensitive substring). `createSearchPanel({sources, onResult, maxHits, debounceMs})` builds `#searchInputField` + `#searchResultsTable`; debounced input (100 ms default); HTML-escaped output with `<span class="searchResultHighlight">` on the match span. Click → `onResult(item)`.

**214/214 tests green.** `npm run check` (typecheck + lint + prettier + vitest) clean.

**Phase 4.5 — audio MVP added this session (238/238 tests green):**

- `src/types/data.d.ts` — appended `TapeRange` + `TapeRangesData` ambient interfaces.
- `src/types/mission.d.ts` — added optional `MissionFeatures { mocrviz?: boolean }` on `MissionConfig`.
- `src/missions/{11,13}.config.ts` — `features: { mocrviz: true }`.
- `src/data/tapeRangesData.ts` (6 tests) — `parseTapeRangesData(rows)` + `loadTapeRangesData(mediaRoot)` + `findTapeForGet(data, channel, seconds)`. Pipe-CSV `tapeId|channelBank|startTimeStr|endTimeStr`. Routes channel ≤ 30 → HR1, > 30 → HR2 (mirrors legacy `getTapeByGETseconds`). Sentinel `T999` rows are retained by the parser and skipped by the audio controller.
- `src/panels/mocrviz/channels.ts` — A13 channel catalog ported verbatim from legacy `cTrackInfo` (60 entries with labels + role descriptions). Available + redacted lists. A11 catalog is a placeholder stand-in (TODO when A11 MOCRviz is verified in browser).
- `src/panels/mocrviz/urls.ts` (8 tests) — pure URL builders. A13: `DA13_{tape}_{bank}_16khz_mp3_16/DA13_{tape}_{bank}_CH{ch}.mp3`. A11: `{tape}_defluttered_mp3_16/defluttered_A11_{tape}_{bank}_CH{ch}.mp3`. Channel > 30 → sub-channel index `ch - 30` in the filename. Waveform `.dat` lives in `audiowaveform_512/` sibling folder.
- `src/panels/mocrviz/audio.ts` (10 tests) — `MocrvizAudioController` class. Owns one `HTMLAudioElement` (factory injectable for tests via `HtmlAudioLike` interface). `tick(currentGetSeconds, isPlaying)` resolves desired tape via `findTapeForGet`, reloads `src` on tape boundary cross, syncs play/pause, seeks when drift > tolerance (legacy 2 sec). Treats `T999` as "no audio". `setChannel(n)` forces tape resolution on next tick.
- `src/panels/mocrviz/index.ts` — `createMocrvizPanel({container, mission, mediaRoot, audioRoot})` async mounter. Renders status readout (channel / tape / GET) + native `<audio controls>` element + clickable channel button grid (redacted/unavailable channels are disabled). Returns `{setClock, setChannel, getChannel, destroy}`.
- `src/dev/missionHarness.ts` — `mountMocrvizPanel(config)` dynamically `import()`s the panel module (lazy-load), gated on `config.features?.mocrviz === true` and `config.id in {11, 13}`. Card hidden until gate passes. Hooks into existing `startTicker`.
- **Phase 4.5b (deferred):** waveform canvas + playhead overlay (peaks.js replacement), isometric MOCR console graphic, transcript loader (separate from main app's transcript), tape activity heatmap. Each is its own ~half-day unit and the audio path is the gating user value for the exit criterion.

**Next action:** Phase 4.5b OR continue toward Phase 6 (production HTML shell + CSS). Recommend Phase 6 prep first — gather Ben's sign-off that the MOCRviz audio MVP behaves correctly in browser at the listed snapshot GETs, then either fold 4.5b in alongside Phase 6 styling or schedule it explicitly.

**Browser app surface (verification layers 1 & 2):** `/11/`, `/13/`, `/17/` (via `src/app/missionApp.ts`) mount **all 11 data modules** alongside the navigator renderer: live GET readout, config dump, Paper.js `NavigatorRenderer`, TOC list, mission-stages list, video-segments readout, commentary readout, utterance readout, photo readout, video-URL readout, crew-status readout, telemetry readout, orbit readout. Each section shows count + the entry covering the current historic-launch GET, refreshed once per second. Page is a diagnostic readout today; Phase 6 grows it into the real mission shell.

From this point on, every typed engine/panel landed in `src/` must be wired into `src/app/missionApp.ts` (append a `<li>` to the "typed modules wired in" list and mount it) before the work is considered done. Pristine legacy at `/legacy/{N}/` is the production-equivalence oracle; no Playwright baselines are captured against the typed app yet (Phase 6). The `/dev/` raw harness and the `/legacy/` oracle plugin are dev-only and never ship.

**Reminder — the wire-up lesson (Phase 4):** Do NOT shim legacy globals via `window.X` from `src/main.ts`. (1) In prod the head builder is called with `{ includeEsmEntry: false }` — `src/main.ts` doesn't load. (2) Even in dev, `<script type="module">` is deferred to run AFTER classic scripts, so legacy `index.js` handlers already register first. Phase 4/5 modules stay as parallel typed references until the caller itself converts to ESM in Phase 5.

**Realistic path through remaining Phase 4.5 / 5 / 6 / 7 / 8 work (multi-session):**

1. **Navigator data overlays (additive):** add tick/rectangle layers to `renderer.ts` that consume the typed loaders. Mission-stage color bands, video-segment rectangles, photo ticks, TOC ticks — pure additive; doesn't change the existing renderer surface or break any existing tests.
2. **Phase 4.5 MOCRviz** — iframe-to-ESM is runtime-changing; needs Ben's verification gate before and after. Will subsume per-channel MP3 playback (`MOCRviz/MOCRviz.js` `gPlayer` + `loadChannelSoundfile()` + `peaks.js`).
3. **Phase 5 panels** (TOC, transcript, commentary, photos, telemetry, crew, dashboard, search) — each one converts a legacy panel to a typed ESM module + browser test, then deletes the legacy call sites. Plan declares Playwright visual diff blocking from this phase onward.
4. **jQuery removal** — follows the last panel conversion; delete the dep.
5. **Phases 6 / 7 / 8** — CSS unification + responsive, cutover, cleanup. Gated on Phase 5 finishing.

**Next action (agent):** The data-loader track is complete. Best next step is the **navigator data overlays** — these are pure additive extensions to `renderer.ts` that draw mission-stage color bands, video-segment start-tick rectangles, photo ticks, and TOC ticks using the already-loaded typed data. No legacy changes. Start with stage color bands (use `missionStagesData` to paint tier background colors), then video-segment boundary ticks, then photo/TOC ticks. Each overlay gets unit tests and is wired through the existing `/dev/{N}/` navigator section. Hold MOCRviz / panel extractions for Phase 4.5 / 5 with Ben's gate.

**Notes from this session (audit + refactor):**

- Previous agent run added all 7 remaining CSV loaders (`commentaryData`, `utteranceData`, `photoData`, `videoUrlData`, `crewStatusData`, `telemetryData`, `orbitData`) and their tests, but left 3 bugs:
  1. `videoUrlData.ts` had a `string | undefined` assignment error on the 3-col branch (used `row[0]` instead of `col0` which was already narrowed). Fixed.
  2. `commentaryData.test.ts`, `photoData.test.ts`, `utteranceData.test.ts` used timeStr format (`"001:00:00"`) in fixture timeId fields — `timeIdToSeconds` expects no-colon format (`"0010000"`), so the `findClosest*` tests returned -1 instead of 0. Fixed all 3 fixture sets.
  3. None of the 7 new data modules were wired into `missionHarness.ts` (violating the "wire before done" rule). Added imports + 7 HTML sections + 7 `mount*` functions + 7 `void mount*()` calls. Each section shows count + current entry at historic-launch GET, live-updated every second.
- 167/167 tests green; `npm run test:all` clean after fixes.

**Notes from earlier sessions:** _(preserved below)_

- Added `missionStagesData.ts` + `videoSegmentData.ts` + 23 tests; all wired into `/dev/{N}/`. 131/131 tests green.
- `findStageIndex` returns `-1` at or after `endSeconds` of the last stage. `findVideoSegmentIndex` treats `NaN` `endSeconds` as open-ended.

**Notes from this session (Phase 3):**

- `MissionConfig` extended with `meta` (title, description, ogImage, ogUrl, fbAppId) and `head` (paperVariant, includeYouTubeIframeApi, stylesCacheBust, extendedFavicons, manifestFile). All three configs populated to reproduce each mission's legacy head verbatim.
- New: `src/template/head.ts` — `renderMissionHead(config, { includeEsmEntry })` builds the shared head HTML.
- New: `src/dom/index.ts` — typed DOM shim (`qs`, `qsa`, `on`, `delegate`, `ready`, `setText`, `setHtml`, class/attr helpers, `show`/`hide`).
- New: `src/main.ts` — ESM bootstrap entry.
- `vite.config.ts`: `injectMissionConfig` plugin now REPLACES the full `<head>...</head>`. Build path passes `{ includeEsmEntry: false }`.
- A17 `<title>` text preserved verbatim; og:title/og:site_name use title-cased form.

---

## Phase status

| Phase                            | Status                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Git init + subtree imports       | done                          | All 4 repos on master; LFS migration + prune applied                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 0 — Scaffold + baseline capture  | done                          | Vite/TS/ESLint/Prettier/Vitest/Playwright pinned; 54 prod baselines captured (Windows host)                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1 — Lift A13                     | done                          | All three missions (A11, A13, A17) files lifted to `public/{N}/` and `legacy-src/{N}/`. Dev boots clean. Ben confirmed on all three.                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2 — Mission config (typed)       | done                          | Typed `{11,13,17}.config.ts`; `window.MISSION` wired; KeyCDN stripped. Ben confirmed all three.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3 — Unified entry + jQuery start | done                          | Shared head builder + ESM entry + jQuery shim landed. `npm run check` + `npm run build` green. Ben confirmed head is correct, all 3 missions load clean.                                                                                                                                                                                                                                                                                                                                                                                      |
| 4 — Extract engines              | substantially done            | All five planned reference modules landed: `clock` (28 tests), `csvLoader` (13), `ytplayer` (3), `navigator/layout` (41), `navigator/renderer` + `paperApi` (8 tests, mounted on `/dev/{N}/` against live GET). Audio scheduler reclassified to Phase 4.5 (lives inside MOCRviz). Legacy `index.js`/`ajax.js`/`navigator.js` unchanged (consumed in Phase 5). Awaits Ben's pass-through of the renderer on `/dev/{N}/` before formally marking `done`. Navigator data overlays (stage/video/photo/TOC ticks) deferred to Phase 5 data wiring. |
| 4.5 — MOCRviz refactor           | audio MVP done; 4.5b deferred | Audio half landed in `src/panels/mocrviz/` and mounted in `/dev/{11,13}/`. Typed channel grid + HTMLAudioElement synced to typed clock (tape boundary reload, drift-tolerant seek, T999 sentinel handling). 24 new unit tests (238/238 green). Waveform canvas + isometric MOCR + transcript loader + tape activity → Phase 4.5b. A11 channel catalog is a placeholder copy of A13's — fill in from `public/11/MOCRviz/MOCRviz.js` `cTrackInfo` when A11 MOCRviz is verified.                                                                 |
| 5 — Panels + finish jQuery       | done (in dev)                 | All three sub-tracks complete in `src/`: **Track A** — 11 typed loaders, 175 tests. **Track B** — navigator overlays, verified in browser. **Track C** — all 8 typed panels (`toc`, `commentary`, `transcript`, `photo`, `telemetry`, `crewStatus`, `dashboard`, `search`). 214/214 tests green. Ben confirmed `/dev/{N}/` panels work. **"Finish jQuery" sub-goal dropped per strategy rewrite** — no `src/**` file uses jQuery; legacy `public/{N}/*.js` (which does) is reference-only and deleted in Phase 7.                             |
| 6 — CSS + responsive             | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 7 — Cutover                      | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 8 — Cleanup + future missions    | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P-1 Pipeline scaffold (uv)       | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P-2 CSV schema + data-inputs     | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P-3 A13 ingestion                | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P-4 A11 + A17 ingestion          | not started                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## Decisions made during execution

_(Record here any deviations from the plan, on-the-fly decisions, or
discoveries that change how a future phase should be done.)_

| Date       | Phase    | Decision / Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-29 | URL swap | Typed app moved from `/dev/{N}/` to `/{N}/`. Lifted-legacy HTMLs moved to `legacy-oracle/{N}/` and served at `/legacy/{N}/` via a dev-only Vite plugin that rewrites asset requests to `public/{N}/`. `src/main.ts`, `src/template/head.ts`, `src/entries/{a11,a17}.ts`, and the `copyLegacyHtml` / `injectMissionConfig` Vite plugins all deleted (dead). `src/dev/missionHarness.ts` renamed to `src/app/missionApp.ts`; `dev/shared.css` renamed to `src/app/app.css`. `dev/{11,13,17}/` deleted.                                                                                                                            |
| 2026-05-29 | Restore  | Lifted legacy under `public/{N}/` and `legacy-oracle/{N}/index.html` and `legacy-src/{N}/` had been edited in place during Phases 1–2 (index.js rewired to `window.MISSION`, hand-crafted `<head>` blocks emptied). All three restored verbatim from sibling repos at `F:/_repos/Apollo_11`, `F:/_repos/Apollo_13`, `F:/_repos/Apollo17.org`. `mobile/` (all), `spacecraft_dev/` (A13), `nominee/` (A17) excluded per locked decisions. `/legacy/{N}/` now matches `apolloinrealtime.org/{N}/` byte-for-byte (modulo CRLF on a few files). 238/238 tests still green.                                                            |
| 2026-05-28 | Git init | All 4 legacy repos on `master`; subtrees imported oldest-first (A17→A11→A13→landing)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-05-28 | Git init | `git lfs migrate import --everything --above=5mb` — 1,464 commits rewritten; large Premiere Pro, OCR, PDF, Sketch files now in LFS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-05-28 | Git init | `git lfs prune` removed 14 transient LFS objects (deleted-file blobs that had no branch-tip reference)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-05-28 | Git init | Decision in `07-repo-and-git-strategy.md` ("do not run filter-repo to strip assets") updated: we DO strip transient blobs via LFS + prune, but the original legacy repos are untouched                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-05-28 | Phase 0  | Toolchain pinned with exact (no-caret) versions per longevity principle (A2b). Lockfile committed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-05-28 | Phase 0  | Playwright baseline capture uses `toMatchSnapshot()` (single-shot) instead of `toHaveScreenshot()` because live mission clock at pre-launch/launch never produces two identical frames.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-05-28 | Phase 0  | Baselines captured on Windows host; filenames include `-win32` suffix. Re-capture on cutover host (likely Linux CI) before Phase 7.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-28 | Phase 1  | A13 `index.html` placed at project root (`13/index.html`), not in `public/13/`. Vite's middleware does not serve `public/*/index.html` for directory URLs; without a root entry, Vite SPA-falls-back to the landing `index.html`. Pattern to repeat for A11/A17: HTML at root, assets in `public/{N}/`.                                                                                                                                                                                                                                                                                                                         |
| 2026-05-28 | Phase 1  | Copied full `_webroot/13/` into `public/13/` (14 MB incl. img/, indexes/, MOCRviz/) so the app is self-contained in dev. Phase 2 will strip KeyCDN references in `index.js`.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-05-28 | Phase 2  | `window.MISSION` injected via Vite `transformIndexHtml` (`head-prepend`) keyed off `/{NN}/index.html` URL. Chosen over a generated public JS file so the TS config stays the single source of truth. Pattern works for dev and build. Legacy `index.js` files keep their `var c*` names but source from `window.MISSION`.                                                                                                                                                                                                                                                                                                       |
| 2026-05-28 | Phase 4  | Do NOT shim legacy globals via `window.X` set from `src/main.ts`. Two reasons: (1) build path uses `{ includeEsmEntry: false }` so `main.ts` doesn't load in prod, and (2) `<script type="module">` is deferred so even in dev it runs after classic `index.js` handlers register. Phase 4 extractions are reference implementations; legacy callers stay on their inline copies until the caller itself converts to ESM (Phase 5).                                                                                                                                                                                             |
| 2026-05-28 | Phase 4  | Added a new "progressive in-browser" verification layer to `05-migration-plan.md` (§Verification, layer 2): per-mission dev pages at `/dev/{11,13,17}/` boot through `src/main.ts` against the typed `MissionConfig` and load ONLY the typed ESM modules that exist so far — no legacy `index.js`/`ajax.js`/`navigator.js`. Each typed engine/panel must be wired into the matching `/dev/{N}/` page before being considered done. Legacy `/{N}/` pages remain the production-equivalence oracle; no Playwright baselines are captured against `/dev/{N}/`. Pages will be removed from the production build at Phase 7 cutover. |

---

## Deferred improvements noticed

_(Things worth doing eventually but out of scope for the current phase.
Log them here so they don't get lost.)_

| Noticed in phase | Description |
| ---------------- | ----------- |
|                  |             |

---

## Playwright baseline snapshot inventory

Captured in Phase 0. Delete rows that haven't been captured yet; add rows
as new snapshots are added.

| File                                                                                                                                                                 | Mission  | GET                                       | Viewport                      | Captured           |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------- | ----------------------------- | ------------------ |
| `tests/visual/baseline.spec.ts-snapshots/baseline/a{11,13,17}/{pre-launch,launch,key-event-1,key-event-2,final-phase,end}-{desktop,tablet,phone}-baseline-win32.png` | 11/13/17 | per `05-migration-plan.md` snapshot table | 1440×900 / 768×1024 / 390×844 | 2026-05-28 (54/54) |

---

## Session log

_(Brief one-liner per session. Newest at the top.)_

| Date       | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-29 | Full assessment + URL swap + legacy restore. (1) Moved typed app from `/dev/{N}/` to `/{N}/`. Pristine legacy oracle now at `/legacy/{N}/` via a dev-only Vite plugin that rewrites asset requests to `public/{N}/`. Deleted dead code: `src/main.ts`, `src/template/head.ts`, `src/entries/{a11,a17}.ts`. Renamed `src/dev/missionHarness.ts` → `src/app/missionApp.ts`, `dev/shared.css` → `src/app/app.css`. Updated `vite.config.ts` with `legacyOraclePlugin` and listed `{11,13,17}/index.html` in `rollupOptions.input`. (2) Discovered the lifted legacy under `public/{N}/`, `legacy-oracle/{N}/`, and `legacy-src/{N}/` had been edited in place during earlier phases (`index.js` rewired to `window.MISSION`; hand-crafted `<head>` blocks emptied). Restored all three missions verbatim from sibling repos at `F:/_repos/Apollo_11`, `F:/_repos/Apollo_13`, `F:/_repos/Apollo17.org`, excluding retired `mobile/`, `spacecraft_dev/`, `nominee/` subdirs. (3) Rewrote `docs-plan/05-migration-plan.md` Strategy + verification + Phases 5–7 to reflect new URL layout (no more `/dev/{N}/`, no atomic Phase 7 swap; production cutover is just shipping `dist/`). (4) Added `legacy-oracle/README.md`. 238/238 tests green; `npm run check` green; build green. |
| 2026-05-28 | Phase 5 Track C kickoff. First panel extracted: `src/panels/toc/index.ts` — typed, jQuery-free, iframe-free replacement for `TOC.html` + legacy `scrollTOCToTimeId`. Builds the same DOM shape (`.TOC_container`, `ul.TOCli.TOC{1,2}`, `li.tocitem#tocid{timeId}`, `.TOCTimestamp`, `.TOC_item`) so existing CSS in `styles.css` continues to apply. Exposes `setActiveTimeId(timeId \| null)` (highlight + `scrollIntoView`) and `onSeek(timeId)` callback (replaces `parent.seekToTime(...)` inline strings). Pure helper `groupTocEntries` splits entries into per-level `<ul>` runs — 4 Vitest tests. Wired into `/dev/{N}/` as a real panel section. Also fixed 2 pre-existing lint errors (`@typescript-eslint/unbound-method`) in `tests/unit/navigatorRenderer.test.ts` that were blocking `npm run check`. 179/179 tests green. Tracker updated. |
| 2026-05-28 | Phase 5 Track B landed. Extended `renderer.ts` with `drawTierOverlays()`: stage ticks (tier 1 + tier 2), video-segment rectangles (tier 1 + tier 2, viewport-clipped), photo ticks (tier 1 + tier 2), half-hour time ticks (tier 2), TOC ticks + level-1 labels (tier 2). New `NavigatorOverlays` interface in `navigator.d.ts`. `missionHarness.ts` loads the 4 datasets concurrently via `Promise.allSettled` and passes result as `overlays`. 8 new tests — 175/175 total green. Manual browser verification of overlays on `/dev/{N}/` is the next step before marking Track B done and starting Track C.                                                                                                                                                                                                                                             |
| 2026-05-28 | Doc review + alignment pass. Assessed all docs-plan files against implementation. Deviations found and corrected in `05-migration-plan.md`: (1) Phase 4 exit criterion no longer claims `index.js` is removed (wire-up lesson — implementation correct, plan wrong). (2) Phase 4.5 gained a Ben verification gate note. (3) Phase 5 restructured into three named sub-tracks (Track A ✅ done, Track B overlays next, Track C panels+jQuery). (4) `src/types/csv.ts` reference renamed to `src/types/data.d.ts` to match actual implementation. No code changes; all 167 tests still green.                                                                                                                                                                                                                                                               |
| 2026-05-28 | Audit + refactor of prior-model run. Previous model added all 7 remaining CSV loaders but left 3 bugs: (1) `videoUrlData.ts` TS2322 on `row[0]` in 3-col branch — fixed by using narrowed `col0`. (2) `commentaryData`, `photoData`, `utteranceData` test fixtures used timeStr format in timeId fields — fixed all 3 to no-colon format. (3) None of the 7 new modules were wired into `missionHarness.ts` — added imports, 7 HTML sections, 7 `mount*` functions, 7 `void mount*()` calls. 167/167 tests green; `npm run test:all` clean. Tracker updated.                                                                                                                                                                                                                                                                                              |
| 2026-05-28 | Phase 5 data-loader track continued. Added `src/data/missionStagesData.ts` (loader/parser + `findStageIndex` binary search; back-fills `endSeconds` from next-stage start, last stage from `missionDurationSeconds`) + 10 unit tests. Added `src/data/videoSegmentData.ts` (loader/parser + `findVideoSegmentIndex` binary-search-plus-range-check; NaN `endSeconds` treated as open-ended) + 13 unit tests. Both wired into `/dev/{N}/` with live highlights driven by the historic-launch GET. 131/131 tests green; `npm run test:all` clean. Pushed back on user request to skip ahead to Phases 6/7/8 (Phase 7 cutover needs Phase 5 panels + jQuery removal + Playwright parity first); continued Phase 5 in order. Tracker resume block deduplicated.                                                                                               |
| 2026-05-28 | Confirmed `npm run test:all` is clean (no errors despite user report). Phase 4 renderer + `paperApi` + 8 renderer tests already landed in a prior session and are mounted on `/dev/{N}/` against `paper-full.js`. Started Phase 5 additive data layer: `src/data/tocData.ts` (typed loader + parser for `TOCData.csv`, mirrors legacy `processTOCData`) + `findClosestTocIndex` (binary-search nearest-entry, safe across negative-second boundary) + 14 unit tests. Wired into `/dev/{N}/` as a scrollable TOC list with a live highlight driven by the historic-launch GET. 108/108 tests green; `npm run test:all` green. Tracker resume block + phase rows updated.                                                                                                                                                                                   |
| 2026-05-28 | Verification layer 2 scaffolded: per-mission progressive dev pages at `/dev/{11,13,17}/`. New files: `dev/{11,13,17}/index.html`, `dev/shared.css`, `src/dev/missionHarness.ts`. Each page sets `window.MISSION` from the typed config, shows mission name + live GET readout (modern-year + historic launch) via `src/shell/clock.ts`, dumps the full config, and exposes a "typed modules wired in" checklist for follow-on engines/panels to mount into. `npm run check` green (86/86); curl smoke shows all dev + legacy URLs return 200.                                                                                                                                                                                                                                                                                                             |
| 2026-05-28 | Phase 4 cont: `src/engines/navigator/layout.ts` extended with `tierForY` + `hitTestMouseMove` (clamped, with legacy tier2/tier3-boundary gap) + `hitTestMouseClick` (unclamped, `else`-tier-3 fall-through). +18 tests (41 total in layout suite, 86/86 overall). Audio scheduler reclassified to Phase 4.5: A11/A13/A17 parent apps have no scheduler — per-channel MP3 playback lives entirely inside `MOCRviz/MOCRviz.js` (`gPlayer` + `loadChannelSoundfile()`).                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-05-28 | Phase 4 cont: `src/data/csvLoader.ts` (pipe-delimited CSV fetch + parse + cache-bust) + 13 tests; `src/engines/ytplayer/index.ts` (typed YouTube IFrame API loader: promise + caching + chain-onto-existing-callback) + 3 tests. 45/45 tests green. Legacy `ajax.js`/`index.js` untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-05-28 | Phase 4 lesson: attempted to delegate legacy `secondsToTimeStr` etc. to `window.MissionClock` set by `src/main.ts`; reverted. Build path has `{ includeEsmEntry: false }` so `main.ts` is dev-only; deferred `<script type="module">` also breaks classic-script timing. Phase 4 modules stay as parallel typed references until Phase 5 converts call sites.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-28 | Phase 4 start: `src/shell/clock.ts` extracted (typed pure fns: `padZeros`, `secondsToTimeStr`, `secondsToTimeId`, `timeIdToSeconds`, `timeIdToTimeStr`, `timeStrToTimeId`, `timeStrToSeconds`). 28 Vitest unit tests; `npm run check` green (29/29). Legacy `index.js` files left unmodified pending Ben's Phase 3 verification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-05-28 | Phase 3: shared `<head>` builder (`src/template/head.ts`) replaces per-mission heads via Vite plugin (dev + build). `src/dom/index.ts` jQuery shim + `src/main.ts` ESM entry land. `npm run check` + `npm run build` green; all 3 missions smoke clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-05-28 | Phase 2 (A11 + A17): webroots lifted to `public/{11,17}/` + `legacy-src/{11,17}/`; typed `{11,17}.config.ts` + `window.MISSION` wired; legacy `var c*`/`var g*` blocks read from it; KeyCDN stripped. `npm run check` green; all 3 missions smoke clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-05-28 | Phase 2 (A13): typed `MissionConfig` + `13.config.ts`; Vite `transformIndexHtml` plugin injects `window.MISSION`; `public/13/index.js` reads from it; KeyCDN stripped. `npm run check` green; A13 smoke clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-05-28 | Phase 1 file-lift: A13 webroot copied to `public/13/` + `legacy-src/13/`; dev server serves A13 clean (no JS errors). Awaits Ben's manual side-by-side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-05-28 | Phase 0: Vite+TS+ESLint+Prettier+Vitest+Playwright scaffold; `npm run check` green; 54 Playwright production baselines captured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-05-28 | Git init; 4 subtree imports; LFS migration (>5MB); 14 transient LFS objects pruned; LEGACY.md added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-05-28 | Planning complete. All 7 planning docs written and locked. `.github/copilot-instructions.md` and this tracker created. Ready for Phase 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
